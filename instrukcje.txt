INSTRUKCJA WDROŻENIA API I TURSO — DUNGEON: ECHOES BELOW
==========================================================

1. GDZIE ZNAJDUJE SIĘ API
-------------------------
API jest częścią tej samej aplikacji Next.js. Nie wstawiaj go do osobnego
serwera PHP/Express ani do katalogu public/. Przy uruchamianiu aplikacji Next
automatycznie udostępnia ono endpointy z katalogu:

  src/app/api/

Aktualne endpointy:
  POST /api/auth/register  body: { "username": "nazwa", "password": "hasło" }
  POST /api/auth/login     body: { "username": "nazwa", "password": "hasło" }
  POST /api/auth/logout
  GET  /api/auth/me
  POST /api/game/save      body: { "state": <aktualny stan gry> }
  GET  /api/game/load

Kod bazy i uwierzytelniania jest w:
  src/lib/server/database.ts
  src/lib/server/auth.ts

2. WYMAGANE ŚRODOWISKO
----------------------
Wymagany jest Node.js 20.9 lub nowszy. Projekt korzysta z pakietu
@libsql/client do połączenia z Turso.

Lokalnie utwórz plik .env.local (jest ignorowany przez Git):

  TURSO_DATABASE_URL="libsql://..."
  TURSO_AUTH_TOKEN="..."

Na Vercel/hostingu dodaj te same dwie wartości jako zmienne środowiskowe dla
Production, Preview i Development. NIE dodawaj tokenu do plików w src/ ani
do repozytorium.

W katalogu projektu uruchom:

  npm install
  npm run dev

Produkcja:

  npm run build
  npm run start

Turso jest zewnętrzną, trwałą bazą libSQL, dlatego aplikację można wdrażać na
Vercel i innych środowiskach serverless. Endpointy API mają runtime Node.js,
nie Edge Runtime.

3. TABELE TURSO/LIBSQL
----------------------
Tabela users przechowuje konto i hash hasła scrypt.
Tabela sessions przechowuje tokeny sesji (ciasteczko HTTP-only).
Tabela game_saves przechowuje jeden zapis JSON dla każdego użytkownika.

Schemat jest tworzony automatycznie przy pierwszym żądaniu API. Gdy trzeba
utworzyć go ręcznie, otwórz konsolę bazy w panelu Turso i uruchom:

  CREATE TABLE IF NOT EXISTS users (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      username TEXT NOT NULL UNIQUE COLLATE NOCASE,
      password_hash TEXT NOT NULL,
      created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
  );
  CREATE TABLE IF NOT EXISTS sessions (
      token TEXT PRIMARY KEY,
      user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
      expires_at INTEGER NOT NULL
  );
  CREATE TABLE IF NOT EXISTS game_saves (
      user_id INTEGER PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
      state_json TEXT NOT NULL,
      updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
  );

4. KOMENDY W GRZE
-----------------
  help
  register <nazwa> <hasło>
  login <nazwa> <hasło>
  logout
  new mage|bandit|knight
  attack
  next
  use healing_potion
  shop
  buy <id>
  leave
  event enter|leave
  upgrade power|armor|agility|luck|health
  status
  stats
  save
  load

Przykładowa pierwsza sesja:
  register gracz01 BardzoTajneHaslo
  new knight
  attack
  save
